# SEITA web — environment
# Demo mode uses localStorage gateways (no backend). Set to 'http' to use the
# Axios HTTP gateways against a real API.
VITE_GATEWAY=storage
VITE_API_BASE_URL=http://localhost:8080/api
